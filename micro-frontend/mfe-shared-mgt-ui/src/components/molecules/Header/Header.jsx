export default function Header() {
  return (
    <header style={{ padding: "16px", background: "orange" }}>
      <h2>Shared Header(JSX Working)</h2>
    </header>
  );
}

// export default Header();
